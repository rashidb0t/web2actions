# Connector Specification

This package defines the schema and format for connector definitions. These specifications allow AI agents to understand and interact with web resources in a standardized way.